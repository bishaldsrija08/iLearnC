#include<stdio.h>
#include<math.h>
int main(){
    int a = 10;
    int b = 20;
    int sum = a + b;
    int power = pow(4,2);
    printf("Sum of %d and %d is %d.\n", a, b, sum);
    printf("4 raised to the power 2 is %d.\n", power);
    return 0;
}